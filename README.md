# tiny-transformer
Learning ML and building a distributed transformer from scratch in C++.

## Building

```bash
make
```

## Running Tests

```bash
# Build and run all tests
make run-tests

# Or build tests separately, then run
make tests
./build/test_tensor
```
